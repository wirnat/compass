# Superpowers Archive

This folder contains historical implementation notes imported from the older Superpowers workflow.

These files are not active Compass policy. Use them only as background context when auditing why a capability was introduced.

Current sources of truth:

- `SKILL.md` for agent-facing behavior and gates.
- `references/` for structured reusable policy.
- `docs/process/` for this repository's active process lock.
- `README.md` for user-facing product behavior.

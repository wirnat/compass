# Architecture

This folder contains documents that explain system shape, technical boundaries, dependency direction, and high-level structure.

Architecture documents should help readers answer:

- what parts the system is divided into
- which boundaries must be protected
- where dependencies should point
- why the structure exists

Start with [[docs/foundation/architecture-principles]] before creating detailed architecture notes.
